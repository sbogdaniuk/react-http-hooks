export const routes = {
  posts: '/posts',
  post: '/post/:id',
  users: '/users',
  user: '/user/:id',
  client: '/client',
  logs: '/logs',
}

export const endpoints = {
  posts: '/posts',
  post: '/posts/:id',
  users: '/users',
  user: '/users/:id',
  comments: '/comments',
  comment: '/comments/:id',
}
